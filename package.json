{
  "name": "medguard-agent-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Local MedGuard agent demo with orchestrator, investigators, approval gate, and audit log.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js"
  },
  "engines": {
    "node": ">=18"
  }
}
