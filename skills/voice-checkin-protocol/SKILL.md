# Voice Check-in Protocol

For the demo, a text transcript stands in for a voice check-in. Flag confusion
or distress terms (`confused`, `dizzy`, `help`, `scared`, `lost`, `unwell`).

This signal recommends a human call decision; it must never directly place a
call or diagnose the patient.
